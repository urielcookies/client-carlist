import React from 'react';

import AddCarForm from '../../AddCarForm/AddCarForm';

const carInfo = (props) => {
  return (
    <div>
      {/* <AddCarForm {...props} /> */}
    </div>
  );
};

export default carInfo;

// what makes you think its ok to steal peoples moeny