import React, {useState} from 'react';
import { Table, Input, Divider, Icon, Header} from 'semantic-ui-react'

const Trip = (props) => {
  // add input listeners
  return (
    <div>
      Trip
    </div>
  )
}

export default Trip;